# Instrucciones para Claude Code - Chuchurex

## 🚀 Deploy Automático

### Frontend (HTML/CSS/JS en `frontend/`)

**MÉTODO ÚNICO:** Git push a GitHub → Cloudflare Pages despliega automáticamente

```bash
git add frontend/
git commit -m "descripción del cambio"
git push origin main
```

**Cloudflare Pages detecta el push y despliega en ~1 minuto**

### Backend (Python/Node en raíz y `pdf-generator/`)

**MÉTODO ÚNICO:** Script deploy.sh

```bash
./deploy.sh
```

El script maneja SSH automáticamente. No preguntes por claves SSH.

---

## ❌ NUNCA HACER PARA FRONTEND

- NO usar `rsync` para frontend
- NO usar `scp` para frontend
- NO usar `ssh root@64.176.19.44` para frontend
- NO preguntar por claves SSH para frontend
- NO intentar subir archivos manualmente al servidor

**El servidor 64.176.19.44 es SOLO para backend API**

---

## ✅ Workflow Correcto

1. Usuario pide cambio en frontend → Haces cambios → `git push origin main` → Listo
2. Usuario pide cambio en backend → Haces cambios → `./deploy.sh` → Listo

---

## 📋 Resumen

| Cambio en | Deploy con | Hosting |
|-----------|------------|---------|
| `frontend/` | `git push origin main` | Cloudflare Pages |
| `app.py`, `pdf-generator/` | `./deploy.sh` | Vultr VPS |

---

## 🎯 Reglas Críticas

1. **SIEMPRE** lee `DEPLOY.md` si tienes dudas sobre deploy
2. **NUNCA** intentes SSH/rsync para frontend
3. **SIEMPRE** usa git push para frontend
4. **NO PREGUNTES** "¿cómo publico esto?" - sigue este documento
5. **DEPLOY AUTOMÁTICAMENTE** cuando el usuario diga "publica", "deploya", "sube a producción"

---

## 📝 Ejemplo Real

```bash
# Usuario: "Agrega un botón en el header y publícalo"

# 1. Editas frontend/index.html
# 2. Sin preguntar, ejecutas:
git add frontend/index.html
git commit -m "feat: add button to header"
git push origin main

# 3. Confirmas al usuario:
# "✅ Cambios publicados. Deploy automático en Cloudflare Pages (~1 min)"
```

---

**Última actualización:** 2026-01-12
**Versión:** 2.0 - Instrucciones explícitas para Claude Code
